server:
  port: 8080

spring:
  # 启用哪一个环境配置, dev为开发环境 test为测试环境  prod为生产环境   uat为用户验收环境
  profiles:
    active:  dev
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://localhost:8080/sparrow_rental?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: ${sparrow.datasource.username}
    password: ${sparrow.datasource.password}
    hikari:
      connection-test-query: SELECT 1 # 自动检测连接
      connection-timeout: 60000 # 数据库连接超时时间,默认30秒
      idle-timeout: 500000 #空闲连接存活最大时间，默认600000（10分钟）
      max-lifetime: 540000 #此属性控制池中连接的最长生命周期，值0表示无限生命周期，默认1800000即30分钟
      maximum-pool-size: 12 #连接池最大连接数，默认是10
      minimum-idle: 10 #最小空闲连接数量
      pool-name: SPHHikariPool # 连接池名称
# 用于打印框架生成的sql语句，便于调试
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 禁用Spring Boot Admin MBean以避免冲突
management:
  endpoints:
    jmx:
      exposure:
        include: ""
  endpoint:
    shutdown:
      enabled: false