<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yulgnier.web.app.mapper.LeaseTermMapper">

    <select id="selectLeaseTermByRoomId" resultType="com.yulgnier.model.entity.LeaseTerm">
        select lt.id,
               lt.month_count,
               lt.unit
        from room_lease_term as rlt
                 left join lease_term as lt on rlt.lease_term_id = lt.id and lt.is_deleted = 0
        where rlt.room_id = #{id}
          and rlt.is_deleted = 0
    </select>
</mapper>
